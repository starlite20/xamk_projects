using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class myScript : MonoBehaviour
{   
    /*
    int score;
    float health = 100, armor = 2, attack = 100;
    float damage = 50;
    byte level;
    bool poisoned;
    int[] values = new int[10];


    [Header("WE CAN HAVE USER DEFINED HEADERS!")]
    [Range(0,10)] //shows slider in unity
    public float speed = 6;

    [TextArea]
    public string textContent = "";

    [HideInInspector]
    public GameObject currentlyLinkedGameObject = null;
    public BoxCollider boxColliderObj = null;
    public Light myLight = null;

    private void Awake()
    {
        currentlyLinkedGameObject = gameObject;
        //or use Get Component template

        boxColliderObj = GetComponent<BoxCollider>();
        //High Load function... Affects Performance

        myLight = GetComponent<Light>();
    }

    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("we begin with unity again");
    }

    // Update is called once per frame
    void Update()
    {
        /*
         * Getkey - Hold or not
         * getkeydown
         * getkeyup
         * 
         */
    /*
        if (Input.GetKey(KeyCode.W))
        {
            transform.Translate(new Vector3(0, 0, speed) * Time.deltaTime);
        }
        if (Input.GetKey(KeyCode.S))
        {
            transform.Translate(new Vector3(0, 0, (-1)*speed) * Time.deltaTime);
        }

        if(Input.GetKeyDown(KeyCode.L))
        {
            myLight.enabled = !myLight.enabled;
            //enabled flag can turn on and off for each component present in the gameobject
        }

        transform.Rotate(new Vector3(0, speed, speed) * Time.deltaTime);
        //transform.localScale = new Vector3(0, 0, 0);

        //if physics involved, use deltatime
    }
        */
}
